--Game

GameState = {
    unknown = "unknown",
    playing = "playing",
    paused = "paused",
    ended = "ended",
}

GameNetSettings = {
    single = "single",
    multiLocal = "multiLocal",
    multiLan = "multiLan"
}

NetRole = {
    unknown = "unknown",
    client = "client",
    server = "server"
}

--Bat

BatPosition = {
    unknown = "unknown",
    left = "left",
    right = "right",
}
